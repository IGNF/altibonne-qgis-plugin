name: Release QGIS Plugin

on:
  push:
    tags:
      - 'version_finale'

jobs:
  build-release:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Delete previous release if exists
        run: |
          gh release delete version_finale --yes || echo "No previous release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}


      - name: Install zip
        run: sudo apt-get install zip -y

      - name: Build plugin zip
        run: |
          mkdir -p IGN_altibonne
          rsync -av --progress . IGN_altibonne/ \
            --exclude .git \
            --exclude .github \
            --exclude IGN_altibonne
          zip -r IGN_altibonne.zip IGN_altibonne

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: "IGN_altibonne.zip"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}